{
  "statusLine": {
    "type": "command",
    "command": "bash -c 'PR=$(gh pr view --json number -q .number 2>/dev/null || echo \"none\"); if [ \"$PR\" != \"none\" ]; then PR=\"#$PR\"; fi; echo \"[Dir: codex_plus | Local: docs/comprehensive-update (synced) | Remote: origin/docs/comprehensive-update | PR: $PR]\"'",
    "timeout": 3
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "SlashCommand/.*",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"event\": \"PostToolUse\", \"tool\": \"TOOL_NAME\", \"status\": \"completed\"}'",
            "timeout": 2
          }
        ]
      }
    ]
  }
}