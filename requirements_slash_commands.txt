# Requirements for Slash Command System TDD Implementation
# Install with: pip install -r requirements_slash_commands.txt

# Core dependencies for slash command functionality
pyyaml>=6.0              # YAML frontmatter parsing in command files
pathlib>=1.0.1           # Path handling for command discovery
typing>=3.7.4            # Type hints for robust code

# Testing dependencies  
pytest>=7.0.0            # Primary testing framework
pytest-asyncio>=0.21.0   # Async test support for command execution
pytest-timeout>=2.1.0    # Timeout protection for long-running tests
pytest-cov>=4.0.0        # Code coverage reporting
pytest-mock>=3.10.0      # Mocking utilities for isolation testing

# FastAPI integration dependencies (already in main requirements)
fastapi>=0.100.0         # Web framework for proxy integration
uvicorn>=0.20.0          # ASGI server for FastAPI
httpx>=0.24.0            # HTTP client for request forwarding

# curl_cffi for Cloudflare bypass (already in main requirements)
curl-cffi>=0.5.0         # Cloudflare bypass capability

# Development and testing utilities
black>=23.0.0            # Code formatting
isort>=5.12.0            # Import sorting
mypy>=1.0.0              # Static type checking
flake8>=6.0.0            # Code linting

# Documentation generation
mkdocs>=1.5.0            # Documentation site generator
mkdocs-material>=9.0.0   # Material theme for docs

# Security testing
bandit>=1.7.5            # Security linting for Python code
safety>=2.3.0            # Check for known security vulnerabilities

# Performance testing
pytest-benchmark>=4.0.0  # Performance benchmarking for tests

# Optional: Enhanced testing capabilities
hypothesis>=6.80.0       # Property-based testing for edge cases
factory-boy>=3.3.0       # Test data factories
freezegun>=1.2.0         # Time mocking for time-sensitive tests